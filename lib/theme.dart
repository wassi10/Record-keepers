import 'package:flutter/material.dart';

const primaryColor = Color(0xFF1A237E);
const secondaryColor = Color(0xFF59706F);
const whiteColor = Color(0xFFFFFFFF);
const blackColor = Color(0xFF272726);


const defaultPadding = EdgeInsets.symmetric(horizontal: 30); // for default padding

TextStyle titleText =
TextStyle(color: primaryColor, fontSize: 32, fontWeight: FontWeight.w700);

TextStyle subTitle = TextStyle(
    color: secondaryColor, fontSize: 18, fontWeight: FontWeight.w500);

TextStyle textButton = TextStyle(
  color: primaryColor,
  fontSize: 18,
  fontWeight: FontWeight.w700,

);